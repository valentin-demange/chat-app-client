import { createContext } from 'react';
import { Chat } from './customTypes';

export const CurrentUserContext = createContext({} as any)
export const CurrentChatContext = createContext({} as any)
export const SetCurrentChatContext = createContext({} as any)

